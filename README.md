# Craftertech Landing Page

Standalone Vite/React project for deploying only the Craftertech marketing landing page.

## Scripts

```bash
npm install
npm run dev
npm run build
npm run preview
```

The deployable static output is generated in `dist/` after `npm run build`.
